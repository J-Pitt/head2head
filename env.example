# Same Upstash Redis as ../truthordare — rooms use a separate key prefix (head2head:)
# Run: npm run setup:env
# UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token
